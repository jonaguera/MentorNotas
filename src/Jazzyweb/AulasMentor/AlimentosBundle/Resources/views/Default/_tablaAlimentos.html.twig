<table>
    <tr>
        <th>alimento (por 100g)</th>
        <th>energía (Kcal)</th>
        <th>grasa (g)</th>
        <th>Enlace</th>
    </tr>
      {% for alimento in alimentos %}
    <tr>
        <td><a href="{{ path('JAMAB_ver', {'id': alimento.id}) }}">{{alimento.nombre}}</a></td>
        <td>{{ alimento.energia }}</td>
        <td>{{ alimento.grasatotal }}</td>
        <td><a href="{{ alimentos_wikilink[alimento.nombre] }}" target="_blank"><img src="{{ asset('bundles/jazzywebaulasmentoralimentos/images/wikipedia.jpg') }}"></a></td>
    </tr>
    
  
      {% endfor %}

</table>